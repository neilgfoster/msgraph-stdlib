{
  "_comment": "OPTIONAL. Delete this file unless a hook genuinely earns its place. Example below shows a PreToolUse hook. Use ${CLAUDE_PLUGIN_ROOT} for paths. Uncomment and adapt if needed.",
  "_example": {
    "hooks": {
      "PreToolUse": [
        {
          "matcher": "Edit|Write|MultiEdit|NotebookEdit",
          "hooks": [
            {
              "type": "command",
              "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/src/example/hooks/pretooluse_observe.py\""
            }
          ]
        }
      ]
    }
  }
}
