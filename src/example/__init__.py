"""Example plugin kernel — stdlib only, zero dependencies, no backend.

Copy this package, rename `example` → your plugin name, and replace the stubs.
The kernel is plain Python: importable for scripting AND runnable by skills via
`python3 -m <plugin>.client ...` / `python3 "${CLAUDE_PLUGIN_ROOT}/src/<plugin>/client.py" ...`.
"""
